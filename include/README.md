# include

複数のソースファイルから共有するプロジェクトヘッダーを置くディレクトリです。

現在、ファームウェアは `src/main.cpp` のみで構成されているため、プロジェクト固有の
ヘッダーはありません。機能を分割する際は `#include "example.h"` のように参照できる
宣言をここへ置き、実装は `src/` または `lib/` に置いてください。

PlatformIO はこのディレクトリを既定の include path として扱います。
