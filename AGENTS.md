# AGENTS.md

このファイルはリポジトリ全体に適用される、開発者および自動化エージェント向けの
継続的な作業指針です。より深いディレクトリに `AGENTS.md` が追加された場合は、
その範囲ではより深いファイルの指示を優先してください。

## プロジェクトの前提

- ESP32 / Arduino framework を PlatformIO でビルドするファームウェアです。
- 現在の実装は `src/main.cpp` に集約されています。
- GPIO 27 の LEDC 出力は外部機器へ接続され得るため、周波数、デューティ比、停止時の
  出力状態を変える変更はハードウェア影響のある変更として扱ってください。
- README は利用者向けの入口、`docs/architecture.md` はコードから読み取れる実装詳細と
  制約の記録です。将来の予定と実装済みの機能を混同しないでください。

## 変更前の確認

1. このファイルと、変更対象までの経路にある追加の `AGENTS.md` を読む。
2. `git status --short --branch` で既存の作業を確認し、他者の変更を上書きしない。
3. `src/main.cpp` と `platformio.ini` を読み、対象機能の実際の挙動を確認する。
4. コードとドキュメントが食い違う場合は、推測で動作を断定せず、現状と意図を明記する。

## 実装方針

- モードは `SERVER`、`SPEED`、`FREQ` のうち 1 つだけを有効にする。
- Arduino / ESP32 の既存 API と命名に合わせ、インデントは 4 スペースとする。
- import / include を例外処理で囲まない。
- GPIO、認証情報、ネットワーク値、速度―周波数変換式を変更した場合は、README と
  `docs/architecture.md` の該当箇所を同じ変更で更新する。
- HTTP 機能を追加・変更した場合は、メソッド、パス、パラメーター、レスポンス、エラー、
  使用例を README または `docs/` に記載する。
- 入力値は GPIO/LEDC API へ渡す前に検証する。負値、ゼロ、オーバーフロー、対応可能な
  周波数範囲を考慮する。
- Wi-Fi 認証情報を追加する場合は、実運用の秘密情報をコミットしない。
- 目的外のリファクタリングや依存関係の更新を同じ変更へ混ぜない。

## 検証

通常の最低限の確認は次のとおりです。

```bash
pio run
git diff --check
```

- モード固有のコードを変更した場合は、そのモードを有効にしてビルドする。可能なら全モードを
  個別にビルドする。
- ハードウェア挙動を変更した場合は、実機でシリアルログと GPIO 27 の波形を確認し、確認した
  ボード、入力、期待値、実測値を変更説明へ記録する。
- 実機や PlatformIO が利用できず検証できない項目は、成功扱いにせず環境上の制約として明記する。
- 自動テストを追加した場合は、README に実行方法を追記する。

## ドキュメント

- 原則として日本語で記述し、コマンド、識別子、API 名はコード上の表記を保つ。
- README の手順はリポジトリルートからコピー＆ペーストできる形にする。
- 未実装機能、既知の不具合、安全上の注意を隠さない。
- コード変更後は、固定値、プロジェクト構成、モード表、既知の制約が最新か確認する。
- 一時的でない新しい運用ルールやリポジトリ固有の知識は、このファイルへ追記する。

## コミットと変更単位

- ひとかたまりの変更につき 1 コミットを目安に、レビュー可能な小さい単位でこまめに
  コミットする。
- コミット前に差分と検証結果を確認する。
- コミットメッセージは変更理由が分かる簡潔な命令形または Conventional Commits 形式
  （例: `docs: document serial input`）を推奨する。
- 生成物（`.pio/` など）やエディター固有ファイルをコミットしない。

## 現在把握している注意点

- 既定の `SERVER` モードには HTTP ハンドラーがなく、制御 API は未実装です。
- `FREQ` モードの非ゼロ入力には `prmWrite` という未定義関数呼び出しがあります。
- `platformio.ini` の Async Web Server 依存関係は現在の同期型 `WebServer` 実装では未使用です。

これらを修正する場合は、修正そのもの、対応するテスト、関連ドキュメントの更新を同じ機能変更の
範囲に含めてください。
